# 三体文明游戏 — 开发文档

> **给接手 Agent 的提醒**：修改代码后，只更新 `docs/` 下职责范围与改动相关的文档；不要为了合规在所有文档中重复同一内容。新增模块或专题可建立独立文档，并从合适的概览文档链接。

> 当前已知逻辑问题、复现证据、修复顺序和验收要求见 [`项目逻辑审查与修复清单.md`](项目逻辑审查与修复清单.md)。

## 项目概述

《三体文明》是一款基于刘慈欣《三体》世界观的三维星际经营模拟游戏。玩家在乱纪元中建立文明，发展科技，应对三体世界的不稳定环境。

### 技术栈

- **引擎**: Godot 4.7
- **脚本语言**: 纯 GDScript
- **3D 渲染**: SubViewport + WorldEnvironment + Camera3D + 程序化 ShaderMaterial + OmniLight3D + ImmediateMesh + GPUParticles3D（星图）；2D 热力图手绘（区域浏览）
- **UI 系统**: Control 节点 + Theme + `_draw()` 手绘列表/图表
- **存档与设置**: JSON（开发期统一读写 `res://saves/` 与 `res://settings.json`）
- **物理**: Jolt Physics（星图交互预留）

> **历史**: 项目原为 Python/Pygame 实现（2026-04）。2026-07 完全重写迁移至 Godot 4。
> 迁移完成度：模拟核心、3D 星图、区域建造、政策与存档交互已完成；现有 17 节点科技树和三面板主页面只是功能原型。起始定居区、勘探、跨区移动/物流和实际开局引导均未实现；设置中的教程开关目前只保存偏好。下一阶段将按设计文档依次实施区域地表观测主页面、知识/科技演化系统、开局难度与稳定纪元转换，再接通完整开局引导和区域扩张。

---

## 目录结构

```
Threebody_Game/
├── project.godot                   # Godot 项目配置 + 3 个 Autoload 注册
├── icon.svg / icon.svg.import      # 项目图标
├── CLAUDE.md                       # Agent 行为指南
├── AGENTS.md                       # Codex 项目级开发与验证规则
├── README.md
│
├── scenes/                         # 场景文件（.tscn + .gd 配对）
│   ├── main.tscn / main.gd                   # 项目入口 → 跳转初始菜单
│   ├── main_menu/
│   │   ├── initial_menu.tscn / .gd           # 标题画面（开始/设置/退出）
│   │   ├── start_game_menu.tscn / .gd        # 新建宇宙 / 继续 / 加载 / 删除
│   │   └── settings_screen.tscn / .gd        # 设置（游戏/显示/音频/控制）
│   ├── game/
│   │   ├── main_screen.tscn / .gd            # 主界面 HUD + 三面板 + 工具栏
│   │   └── game_menu.tscn / .gd              # 暂停菜单（继续/保存/加载/设置/主菜单）
│   ├── shared/
│   │   └── save_browser.tscn / .gd            # 主菜单与暂停菜单共用的可选存档浏览器
│   ├── starmap/
│   │   └── starmap_view.tscn / .gd           # 3D 星图（SubViewport + 轨道摄像机）
│   ├── tech_tree/
│   │   ├── tech_tree.tscn / .gd              # 科技研究界面与 Tooltip
│   │   └── tech_tree_canvas.gd               # 节点/贝塞尔连线/缩放拖拽画布
│   ├── zone_view/
│   │   ├── zone_view.tscn / .gd              # 区域详情/建造/人力分配
│   │   └── zone_map.gd                       # 12×6 可点击热力图
│   └── decision/
│       └── decision.tscn / .gd               # 五项文明政策界面
│
├── scripts/                        # 纯逻辑脚本（不包含 .tscn）
│   ├── autoload/                   # 全局单例（project.godot 注册）
│   │   ├── event_bus.gd            # 全局事件总线（6 个信号）
│   │   ├── game_state.gd           # 游戏状态 — 持有所有模拟子系统并协调更新
│   │   └── save_manager.gd         # 存档管理（扫描/保存/加载/删除）
│   └── simulation/                 # 模拟逻辑层（全部从 Python 移植完成）
│       ├── three_body.gd           # 三体运动模拟（RK4 + 随机微扰 + 轨迹）
│       ├── planet_zones.gd         # 72 区域系统（自转 + 热惯性 + 扩散）
│       ├── entity_manager.gd       # 实体管理器（资源/建筑/人口/电力）
│       ├── tech_tree.gd            # 科技树（5 层 17 节点 + 3 种科研点）
│       └── decision_manager.gd     # 决策管理器（17 种建筑 + 脱水/浸泡）
│
├── resources/                      # 非场景资源
│   ├── configs/
│   │   └── game_config.gd          # GameConfig Resource（基础配置占位）
│   └── themes/
│       ├── default_theme.tres      # 默认 UI 主题（Button/Label/Panel 样式）
│       ├── button_normal.tres      # 按钮常规 StyleBoxFlat
│       ├── button_hover.tres       # 按钮悬浮 StyleBoxFlat
│       └── panel_style.tres        # 面板 StyleBoxFlat
├── tests/
│   └── developer_tools_test.tscn/.gd # 开发者状态修改与项目内存档往返测试
│   └── ui_layout_test.tscn/.gd        # 1080p 关键控件边界与重叠回归测试
│   └── save_browser_test.tscn/.gd      # 存档列表可见选择与双入口复用测试
│   └── subscreen_interaction_test.tscn/.gd # 子界面暂停、按钮稳定性与批量人口测试
│
└── docs/                           # 项目文档
    ├── 开发文档.md                  # 本文档
    ├── 详细参考文档.md               # 类/函数级 API 参考
    └── 游戏设计文档.md               # 游戏机制设计
```

> **已删除的目录**：`data/`（旧 Python 设置/存档）、`assets/`（空目录）、`original_python/`（旧代码归档）—— 均未被任何 Godot 代码引用。

---

## 项目配置

### project.godot 关键设置

| 条目 | 值 | 说明 |
|---|---|---|
| `run/main_scene` | `scenes/main.tscn` | 启动入口 |
| `config/icon` | `icon.svg` | 窗口图标 |
| `window/size/viewport_width` | 1920 | 默认分辨率宽 |
| `window/size/viewport_height` | 1080 | 默认分辨率高 |
| `3d/physics_engine` | Jolt Physics | 物理后端 |
| `rendering/driver.windows` | d3d12 | Windows 渲染驱动 |

### Autoload 注册

| 名称 | 脚本 | 职责 |
|---|---|---|
| `EventBus` | `scripts/autoload/event_bus.gd` | 全局信号（6 个）：screen_changed / game_started / game_loaded / game_paused / time_scale_changed / game_over |
| `SaveManager` | `scripts/autoload/save_manager.gd` | 存档扫描/保存/加载/删除，JSON 格式；开发期固定使用项目可见的 `res://saves/` |
| `GameState` | `scripts/autoload/game_state.gd` | 核心状态持有者，持有 5 个模拟子系统，提供统一 `update()` 与受开发者模式保护的调试修改接口 |

---

## 场景导航流程

```
main.tscn (入口)
  └── InitialMenu （标题画面）
        ├── 开始游戏 → StartGameMenu
        │     ├── 新游戏（命名 + 自动初始存档）→ MainScreen
        │     ├── 继续游戏（最新存档）→ MainScreen
        │     └── 加载存档 → SaveBrowser（分组可选列表）→ MainScreen
        └── 设置 → SettingsScreen

MainScreen （游戏主界面）
  ├── 菜单 → GameMenu （暂停菜单）
  │     ├── 继续 → MainScreen
  │     ├── 保存
  │     ├── 加载 → SaveBrowser（与主菜单相同）→ MainScreen
  │     ├── 设置 → SettingsScreen
  │     └── 返回主菜单 → InitialMenu
  ├── 科技树 → TechTreeScreen
  ├── 政策 → DecisionScreen
  ├── 区域/建筑 → ZoneViewScreen
  ├── 星图 → StarmapView (3D SubViewport)
  └── [开发者模式] 开发者工具 → 人口 / 资源 / 科研点 / 全科技调试
```

`Esc` 在 MainScreen 暂停模拟并打开 GameMenu；在 GameMenu 恢复模拟并返回 MainScreen；在科技树、政策、区域和星图返回 MainScreen。设置界面若从暂停菜单进入，`Esc` 返回暂停菜单。

---

## 更新主循环

```
GameState._process(delta)                       [Autoload，跨场景持续运行]
  │
  └── GameState.update(dt)
      ├── ThreeBodySimulation.update(dt)         RK4 四阶积分 + 轨迹记录
      ├── PlanetZoneManager.update()             自转 + 逐区温度/辐射/光照计算
      ├── EntityManager.update()                 建筑伤害→建造进度→产出消耗→人口增长
      ├── _process_research_output()             科研建筑产出逐帧注入科技点
      ├── _process_storage_damage()              [仅脱水状态] 库存/暴露人口损耗
      └── DecisionManager.update_cooldowns()     决策冷却计时衰减
```

---

## 下一阶段目标架构（规划，本文所列文件与接口尚未实现）

本节用于固定后续实现边界，不能当作当前 API。实施顺序遵循“先主页面观测视图，再知识系统，再难度/稳定纪元”，同时建立可保存的引导状态骨架；完整开局任务和跨区移动在所依赖的区域、知识与场景系统可用后接通。每一阶段都需保持旧存档可读取，并在完成前保留当前可运行原型作为回退。

### A. 区域地表观测主页面

目标是将 `main_screen.tscn` 从三块数据面板改为“区域天空 + 地表 + 边缘 HUD”。不在 `main_screen.gd` 中复制星图 shader 和天体坐标逻辑，先提取共享渲染层：

```
GameState / ThreeBodySimulation / PlanetZoneManager
                  │
                  ▼
CelestialViewRenderer（共享天体材质、日冕、光源、位置同步）
          ┌───────┴────────┐
          ▼                ▼
StarmapView           RegionObservationView
自由宇宙坐标相机       区域地平坐标相机 + 地面遮挡
```

建议新增但尚未创建：

- `scripts/render/celestial_view_renderer.gd`：创建/更新恒星、行星、光源和共享环境，不持有模拟状态。
- `scenes/game/region_observation_view.tscn/.gd`：接收 `zone_id`，计算当地天顶/地平坐标并组合天空、地面和区域建筑视觉层。
- `scenes/game/region_ground_view.gd`：第一版按地形生成空白地表；后续通过 `building_visual_registry` 按建筑状态实例化模型。
- `main_screen.gd` 只负责区域选择、HUD 和导航；当前观察区存入 `GameState.observed_zone_id`。

关键变换：取区域中心法线 `up`，由经纬计算 `east/north`，对 `star.position - planet.position` 归一化后投影到该基底。`dot(direction, up) <= 0` 的天体位于地平线下，不应被天空相机看到。渲染层只消费只读快照，不能反向修改模拟位置。

HUD 重构时，资源/文明/环境数据改为稳定 Label 原地更新，避免重新引入每帧销毁控件的问题。区域选择只改变观察目标；迁都、人口调动和建造保持独立命令。

### B. 知识与科技演化系统

现有 `TechTree` 保留为 `legacy_tech_state` 兼容源。新系统按认知、研究、教育和危机保存拆分职责，避免未来随机事件、遗迹、政策树和 UI 全部耦合进一个大类：

```
KnowledgeSystem（认知状态、传承、衰退、证据）
    ├── DiscoverySystem（自然观察、实验、事件与遗迹线索）
    ├── ResearchProjectSystem（研究人员/设施/材料/时间）
    ├── EngineeringProjectSystem（理论→样机→工艺→应用能力）
    ├── KnowledgeGraph（节点定义、分岔、跨领域依赖、可见性）
    ├── KnowledgePolicySystem（政策树、总体/载体/领域权重、限时计划）
    ├── EducationSystem（教师、学生、课程、教学与师徒实践）
    ├── PreservationAllocator（避难容量、保存清单、确定装载结果与灾害结算）
    └── KnowledgeInheritance（按代保留与灾难冲击）
```

建议新增但尚未创建：

- `scripts/simulation/knowledge_graph.gd`：加载数据驱动节点与不可变 ID，计算直接后继和跨领域依赖。
- `scripts/simulation/knowledge_system.gd`：维护 `HIDDEN/RUMOR/INSIGHT/RESEARCHABLE/RESEARCHING/MASTERED/APPLIED/DEGRADED`。
- `scripts/simulation/discovery_system.gd`：接受观察、实验、生产、随机事件和遗迹来源，输出标准化线索/证据，不直接控制 UI。
- `scripts/simulation/research_project_system.gd`：管理并行研究槽、人员、设施、样本、数据和失败/部分成果。
- `scripts/simulation/engineering_project_system.gd`：管理样机、测试、工艺验证和能力标签。
- `scripts/simulation/knowledge_inheritance.gd`：按代处理教师、学徒、记录副本、实践和灾难造成的保留/衰退。
- `scripts/simulation/knowledge_policy_system.gd`：维护知识政策树、总体重视度、载体/领域优先级、应急姿态和政策历史。
- `scripts/simulation/education_system.gd`：将教师、学生、教学时长、教材和实践设施转换为活态传承结果；岗位仍由统一人口系统扣减。
- `scripts/simulation/preservation_allocator.gd`：读取避难设施的生命保障、干燥库位、体积、承重和能源等容量，生成可预览、可复现的保存分配方案；不得自行读取未来真实轨道来估算伤亡。
- `resources/knowledge/*.tres` 或版本化 JSON：节点、领域、依赖、线索条件、实验、工程化和迁移表；不得继续把完整科技树硬编码在 UI。
- `resources/policies/knowledge/*.tres` 或版本化 JSON：教育、记录、危机保存和灾后恢复政策定义；政策必须引用能力标签和稳定 ID，不得引用节点中文名。

所有外部来源统一通过命令式接口接入，例如：

- `add_clue(node_id, source_id, strength)`
- `add_evidence(node_id, evidence_type, amount, source_id)`
- `record_observation(observation)`
- `start_research_project(node_id, allocation)`
- `start_engineering_project(node_id, design_id)`
- `set_knowledge_priority(priority)`
- `assign_teaching_plan(plan)`
- `preview_preservation_plan(plan, forecast_snapshot)`
- `apply_knowledge_shock(shock)`

随机事件系统只能调用这些入口。知识系统完成状态判定后发出 `knowledge_state_changed`、`branch_revealed`、`knowledge_degraded` 等信号，科技树 UI 订阅只读状态。这样可为大量未来事件和节点预留扩展空间，而无需让事件知道界面节点或研究点实现。

能力消费方通过稳定标签查询，例如 `has_capability("precision_optics")`、`get_capability_level("orbital_observation")`；建筑、星图预测和生产系统不得依赖科技中文名或 UI 可见性。

知识政策界面只向 `KnowledgePolicySystem` 提交制度和权重。日常结算由 `EducationSystem` 消耗真实教师/学生工时、教材和教学设施；危机前由 `PreservationAllocator` 返回确定的床位/库位占用、纳入与未纳入对象。伤亡、受灾区域和知识降级风险必须来自下述 `HazardForecastService` 的文明预测快照；没有对应能力时保持未知或定性描述。玩家确认后才提交不可逆方案。`KnowledgeInheritance` 最终依据实际幸存者、载体损毁和实践中断生成保留结果，禁止政策本身直接增加知识值。

第一版知识政策树只实现四条设计文档已经定义的分支：教育与教学、记录与复制、危机保存、灾后恢复。容量与装载计算必须是给定状态和方案即可复现的纯规则；同一份文明预测快照下反复比较方案不得重掷预测。真实灾害结果只在模拟推进中确定一次，既不能因切换方案重掷，也不能提前泄露给预览层。

### C. 开局难度与稳定纪元

建议新增 `ScenarioManager` 与分阶段轨道提供者：

```
ScenarioManager
    ├── difficulty_id / stable_years / chaos_start_day / seed
    ├── ScenarioDifficultyConfig（开发期可编辑的难度预设目录）
    ├── StableEphemerisProvider（解析二体 + 两颗远星规定星历）
    └── ChaoticNBodyProvider（现有 ThreeBodySimulation RK4）

ObservationNetwork + SatelliteNetwork + KnowledgeSystem
                         │
                         ▼
HazardForecastService（文明可知的轨道情景、区域危害与伤亡/知识损失区间）
                         │
                         ▼
PreservationAllocator / Crisis UI（只显示预测允许公开的字段）
```

- 建议新增 `resources/configs/scenario_difficulty_config.gd`、`difficulty_preset.gd` 和唯一数据资源 `scenario_difficulties.tres`。默认 `easy=100`、`normal=50`、`hard=25`、`extreme=0` 以及自定义上下限都只写在该资源中，不在脚本、场景或 UI 再维护副本。
- 这是随项目提交的开发/平衡配置，不是 `settings.json`、游戏内开发者模式或玩家可修改选项。开发者可用 Godot Inspector 或文本编辑 `.tres`，修改后由配置测试统一校验。
- `StartGameMenu` 在宇宙命名后增加难度步骤；菜单从配置资源生成标准预设，自定义输入使用资源中的上下限校验后再创建游戏。
- `ScenarioManager.create_scenario()` 接收已经解析和校验的预设快照，而不是自行按 ID 查硬编码 `match`。新宇宙把 `difficulty_config_version`、`stable_years` 和 `chaos_start_day` 写入场景状态。
- `ScenarioManager.update(game_time)` 在稳定纪元提供连续位置/速度快照；到 `chaos_start_day` 时将同一快照交给 RK4 模拟器，设置一次性转换标志并发出事件。
- 稳定阶段环境系统仍消费天体快照，不关心快照来自规定星历还是 N 体积分。
- 碰撞检测、轨迹历史、星图与地表天空统一消费当前轨道提供者，禁止每个界面各自判断阶段。
- 新游戏生成时离线验证转换目标状态：稳定期无碰撞、行星围绕主恒星、远星在转换时形成可运行的多体初值；转换之后不再脚本化。
- 默认数据：简单 100 年、普通 50 年、困难 25 年、极限 0 年、自定义 N 年；1 年 = 365 游戏天。这里仅记录设计默认值，运行时唯一来源是 `scenario_difficulties.tres`。
- 配置热改只要求在下一次启动/重新加载项目后影响新游戏，不要求运行中的菜单热更新。已有存档永远使用自身场景快照，禁止加载时用最新预设覆盖。
- 配置资源无效时应阻止新游戏并报告重复 ID、负数/非有限年数、缺失默认项或非法自定义范围，不得用隐藏的脚本常量兜底。

轨道预测能力改为读取 `KnowledgeSystem` 的理论标签、`ObservationNetwork` 的数据质量和 `SatelliteNetwork` 的覆盖率。稳定期二体理论只提高理想轨道理解，不直接给予混沌预测；真实多体偏差出现后才能产生相应发现线索。

建议新增 `scripts/simulation/hazard_forecast_service.gd`，职责是把文明已掌握的能力标签、带误差的观测数据、人口/建筑脆弱性资料和计算资源转换为版本化 `HazardForecastSnapshot`。它不能直接读取 `ScenarioManager` 尚未被文明观测的未来轨道、场景种子或灾害结算结果。

预测显示按能力标签逐步开放：`hazard_warning` 只提供定性风险，`regional_hazard_projection` 提供区域与宽时间窗，`casualty_estimation` 提供伤亡区间，`knowledge_loss_projection` 提供知识载体和节点退化风险。即使标签已解锁，缺少观测基线、仪器校准、卫星覆盖、计算能力或人口/建筑数据时也要降低精度并扩大置信区间。

必须保持两条数据流分离：玩家预览消费 `HazardForecastSnapshot`；灾害实际结算消费 `ScenarioManager` 推进后产生的真实 `HazardOutcome`。预览不得把真实结果传给 UI 再用遮罩隐藏，因为界面刷新、辅助功能或方案差分都可能泄露信息；未知字段应在预测服务输出中就不存在。

### D. 统一存档版本升级

建议在实施上述系统前引入顶层 `save_schema_version` 与逐版本迁移器。目标新增状态至少包括：

- `observed_zone_id`
- `knowledge`、`research_projects`、`engineering_projects`、`discoveries`、`inheritance`
- `knowledge_policy`、`education`、`archive_allocations`、`preservation_plan`
- `scenario`（难度、难度配置版本、稳定年数、阶段、种子、规定星历、转换状态）
- `observation_network`、`satellite_network`
- `hazard_forecasts`（生成时间、输入数据版本、能力层级、情景区间、置信度和过期状态）
- `settlement`（首都、熟悉区、区域认知、聚落/前哨和区域人口/库存）
- `region_operations`（勘探队、迁徙队、运输任务、路线、携带物资和行程进度）
- `opening_guidance`（版本、模式、当前阶段、任务完成、关闭提示和暂缓状态）

旧存档默认：观察区域 `0`；现有 17 科技按迁移表转换；缺少场景阶段的旧局直接进入 `CHAOTIC_NBODY`，不得重置现有天体。

### E. 起始定居、开局引导与区域扩张

当前 `StartGameMenu` 只有 `MAIN/NAMING`，确认名称后直接创建全局人口/资源并进入主页面；`enable_tutorial` 只存在于设置文件。目标流程不在现有菜单脚本里堆叠所有教学逻辑，而是拆成以下职责：

```
StartGameMenu（命名 / 难度 / 引导模式 / 创建）
                        │
                        ▼
OpeningGuidanceController（可保存阶段机，只提交领域命令）
       ├── CapitalSelectionService（只读公开区域认知，确认发源地）
       ├── GuidanceOverlay / Handbook（任务卡、高亮、解释与恢复入口）
       ├── ExplorationSystem（区域认知、勘探队、结果置信度）
       ├── RegionMovementSystem（人口组、路线、时间、风险与到达）
       └── RegionalLogisticsSystem（区域库存、运输、网络与施工供给）
```

建议新增但尚未创建：

- `scripts/simulation/capital_selection_service.gd`：基于文明公开数据生成/比较候选区；不得读取隐藏矿藏或未来轨道评分。
- `scripts/simulation/exploration_system.gd`：维护 `UNKNOWN/OBSERVED/FAMILIAR/SURVEYED/MONITORED` 与带置信度的认知快照。
- `scripts/simulation/region_movement_system.gd`：管理人口组、勘探队和迁徙队在 72 区邻接图上的路线及行程。
- `scripts/simulation/regional_logistics_system.gd`：管理区域库存、运输任务、道路/管线/电网连接和目标区域施工供给。
- `scripts/guidance/opening_guidance_controller.gd`：数据驱动任务状态机，只监听领域事件并请求合法命令，不直接增加资源、解锁科技或移动人口。
- `resources/guidance/opening_guidance.tres` 与任务资源：稳定任务 ID、引导版本、前置、完成事件、提示文本、目标控件语义 ID 和可跳过性。
- `scenes/game/guidance_overlay.tscn/.gd`：可折叠任务卡、控件高亮和引导手册入口；不承载模拟判定。

新游戏创建后先写入可恢复的初始存档，再进入第 0 天暂停的定居阶段。`OpeningGuidanceController` 在完整/精简/关闭三种模式下共享同一世界状态：关闭引导只隐藏讲解，仍必须完成首都选择。任务完成必须来自例如 `capital_confirmed`、`teaching_plan_started`、`construction_started`、`expedition_departed` 等领域信号；单纯打开页面不算完成。

区域推荐和教学提示只消费 `ZoneKnowledgeView`。若两个场景的公开输入相同，即使隐藏矿藏或未来真实轨道不同，候选排序与提示也必须相同。指导层不得持有完整 `PlanetZone` 真值对象再在 UI 隐藏字段。

跨区系统实施前必须先迁移全局池：人口组增加 `current_zone_id/route_id`，库存归属区域或运输任务，建筑工人从当地可用人口分配。迁移按一个版本完成，禁止一段时间同时从全局池和区域池扣除同一资源。跨区施工成本由真实运输和维护结算产生，不保留固定 `3~5 倍` 惩罚。

引导状态随每个存档保存；设置只保存默认引导模式。旧 `enable_tutorial=true/false` 分别迁移为 `FULL/OFF`，旧存档则默认首都区域 `0`、当前人口/库存归入该区，并把不适用于已发展文明的开局任务标为迁移跳过。

---

## 模拟子系统详解

### 1. 三体运动模拟 — `scripts/simulation/three_body.gd`

`class_name ThreeBodySimulation extends RefCounted`

**功能**: 4 天体（3 恒星 + 1 行星）引力模拟，输出环境参数供其他模块消费。

**关键常量**:
- `G = 1.0` — 简化引力常数
- `TRAIL_LENGTH = 200` — 每星轨迹点数

**内部类 StarData**: 持有 mass / position / velocity / color / radius / is_planet / trail

**关键方法**:

| 方法 | 功能 |
|---|---|
| `_init()` | 随机生成层级稳定初始配置（内双星 + 外第三星 + 行星绕双星） |
| `compute_forces_for_state(positions)` | 遍历星对计算引力合力（用于 RK4 四步） |
| `update(dt)` | RK4 四阶积分 + 1e-5 随机微扰 + 轨迹 pop_front |
| `has_collision()` | 检查任意两天体中心距是否小于半径和 |
| `get_environment_params()` | 输出光照强度（归一化）/ 热等级 / 温度 / 辐射 / 稳定性 |
| `get_stars_data()` | 返回 [{position, mass, is_planet}, ...]（供区域系统） |
| `get_planet_position()` | 返回行星坐标 |

**环境参数公式**:
- 光照强度 = `clamp(Σ mass×10/(dist²+100) / 8, 0, 1)`
- 温度 = `-273.15 + total_intensity × 7000`
- 辐射 = `Σ mass×200 / dist^2.5`
- 稳定性 = `1 / (1 + 行星加速度/0.1)`

---

### 2. 行星区域系统 — `scripts/simulation/planet_zones.gd`

`class_name PlanetZoneManager extends RefCounted`

**功能**: 72 区域（12 经度 × 6 纬度）的自转、光照、温度、辐射模拟。

**关键常量**:
- 6 种地形: 平原 / 高原 / 山地 / 峡谷 / 盆地 / 丘陵
- `TERRAIN_RESOURCE_TABLE` — 地形→矿物/肥沃/藻类系数
- `TERRAIN_THERMAL_MODIFIER` — 地形默认温度修正（高原-6°C / 山地-10°C / 盆地+3°C）

**内部类 PlanetZone**: 持有 zone_id / 地形 / 温度 / 辐射 / 光照 / 资源禀赋 / 建筑列表 / 面积权重

**关键方法**:

| 方法 | 功能 |
|---|---|
| `_init(env_config)` | 初始化 72 区 + 构建邻居缓存 |
| `_init_zones()` | 按经纬划分 72 格，随机分配地形和资源（±30% 浮动） |
| `_build_neighbor_cache()` | 每区 ≤4 邻居（经纬相邻，经度环绕） |
| `update(dt, time_scale, stars_data, planet_pos)` | 更新自转角度 → 逐区环境计算 |
| `initialize_temperatures(stars_data, planet_pos)` | 跳过惯性直接设定目标温度 + 10 轮扩散 |
| `_compute_zone_environments()` | 光照（法线 cos + 暗面散射）→ 温度（热惯性趋近）→ 辐射 + 扩散 |
| `_apply_diffusion(dt)` | 区域温度向邻居均值靠拢 |
| `get_average_environment()` | 加权平均（面积权重）返回全局温/辐/光 |
| `get_zone(zone_id)` | 按 ID 返回 PlanetZone |
| `get_all_zones_summary()` | 返回 72 区简要数据列表（2D 热力图渲染用） |
| `add_building_to_zone / remove_building_from_zone` | 维护区域-建筑关联 |
| `get_state() / load_state()` | 序列化 / 反序列化 |

**工作效率函数** (`_calc_work_efficiency`):
- -10°C ~ 40°C → 1.0（满分）
- < -80°C 或 > 100°C → 0.0（无法工作）
- 中间线性插值

---

### 3. 实体管理器 — `scripts/simulation/entity_manager.gd`

`class_name EntityManager extends RefCounted`

**功能**: 管理 7 种资源、所有建筑、人口增长/库存/食物消耗。

**三种内部类**:
- `GameResource` — 单种资源（name / amount / max_storage / add / consume）
- `PopulationManager` — 人口系统（total / breeders / stored_population / 增长 / 饥荒 / 库存操作）
- `GameBuilding` — 单个建筑（17 种类型，耐久 / 产出 / 消耗 / 建造进度 / 环境伤害）

**7 种资源**: 铁矿 / 铜矿 / 稀有矿物 / 藻类燃料 / 化石燃料 / 电力 / 食物

**关键方法**:

| 方法 | 功能 |
|---|---|
| `_init(config)` | 初始化资源池和人口（100 人起始） |
| `add_building(building)` | 添加建筑到列表 |
| `get_resource(name)` | 查资源量，"population" 转发到 PopulationManager |
| `consume_resource / produce_resource` | 资源存取 |
| `get_buildings_by_type(type)` | 筛选活跃同类型建筑（科研产出用） |
| `get_idle_population()` | 总人口 - 生育者 - 建筑工人 |
| `assign_worker_to_building / unassign_worker_from_building` | 工人分配 |
| `assign_breeders / unassign_breeders` | 生育分配 |
| `get_electricity_balance()` | 发电量 vs 耗电量 |
| `update(env_params, zone_manager, dt, dehydrated)` | 重算库存容量 → 环境伤害 → 建造进度 → 产出消耗 → 人口增长 |
| `_process_buildings(dt, zone_manager, dehydrated)` | 电力比惩罚 + 区域效率 + 脱水惩罚 → 产出注入 |
| `get_state() / load_state()` | 全量序列化（含所有建筑详情） |

**建筑产出公式**: `workers × per_worker × durability_ratio × automation × zone_eff × power_ratio`

**人口增长公式**: `breeders × base_growth_per_breeder × food_satisfaction + idle × natural_growth_rate × food_satisfaction`

---

### 4. 科技树 — `scripts/simulation/tech_tree.gd`

`class_name TechTree extends RefCounted`

**当前原型功能**: 5 层 17 节点科技树，3 种科研点数（基础/应用/理论），渐进式注入研究。该实现继续服务当前版本和旧存档，但会按“下一阶段目标架构 B”迁移，不代表最终科技规模或认知规则。

**内部类 TechNode**: 持有 id / name / tier / column / research_cost / resource_cost / prerequisites / unlocked / researching

**17 个科技节点**:

| Tier | 节点 | 科研点需求 |
|---|---|---|
| 0 | telescope（望远镜） | 基础 80 |
| 0 | survival_shelter（维生庇护所） | 基础 60 |
| 0 | basic_metallurgy（基础冶金） | 基础 100 |
| 0 | basic_agriculture（基础农业） | 基础 50 |
| 1 | computer（计算机技术） | 基础 200 + 应用 50 |
| 1 | observatory（天文观测站） | 基础 150 |
| 1 | deep_shelter（深地庇护所） | 基础 120 + 应用 30 |
| 1 | laboratory（实验室） | 基础 180 |
| 1 | basic_electrification（基础电气化） | 基础 100 |
| 1 | fossil_fuel_extraction（化石燃料开采） | 基础 120 |
| 2 | power_plant（火力发电站） | 应用 140 |
| 2 | chaos_prediction（混沌预测模型） | 应用 200 + 理论 50 |
| 2 | automation（自动化控制） | 应用 150 |
| 2 | radiation_armor（防辐射装甲） | 应用 100 + 基础 80 |
| 2 | applied_physics（应用物理） | 应用 200 |
| 2 | material_science（材料科学） | 应用 180 |
| 3 | academy（科学院） | 应用 300 + 理论 100 |
| 3 | high_alloy（高强度合金） | 应用 250 |
| 4 | nuclear_fusion（可控核聚变） | 理论 500 + 应用 300 |

**关键方法**:

| 方法 | 功能 |
|---|---|
| `_init(config)` | 从 config["technology"] 构建节点（无 config 时用硬编码默认） |
| `produce_research(type, amount)` | 产出科技点 → 优先注入当前研究 → 剩余入池 → 检查完成 |
| `start_research(id, entities)` | 扣物质资源 → 标记 researching → 清空 progress |
| `cancel_research()` | 退回已注入点数（物质不退还） |
| `can_start_research(id, entities)` | 检查前置科技/资源/人口/单研究限制 |
| `get_research_progress()` | 当前研究整体进度百分比 + 各类型明细 |
| `is_researchable(id)` | 前置已解锁且自身未解锁未研究中 |
| `get_state() / load_state()` | 解锁列表 + 研究进度 + 科技点数池 |

**科研产出来源**（在 GameState._process_research_output 中）:
- 人口自然产出 = `population / 500 × 天数`
- 研究院（research_institute）→ 基础科研 1.0/工人/天
- 实验室（laboratory）→ 应用科研 2.0/工人/天
- 科学院（academy）→ 理论科研 1.0/工人/天
- 脱水状态 × 0.1

---

### 5. 决策管理器 — `scripts/simulation/decision_manager.gd`

`class_name DecisionManager extends RefCounted`

**功能**: 17 种建筑建造 + 五项文明政策执行。

**内部类 Decision**: 持有 id / category("construction"/"policy") / resource_cost / tech_requirement / worker_capacity / per_worker_output / build_time / storage_capacity...

**17 种建筑**: 藻类采集场 / 藻类食物合成器 / 铁矿场 / 铜矿场 / 稀有矿场 / 农场 / 化石燃料矿井 / 藻类发电站 / 化石燃料发电站 / 庇护所 / 实验室 / 科学院 / 深地庇护所 / 辐射屏蔽站 / 脱水仓 / 大型脱水仓 / 研究院

**关键方法**:

| 方法 | 功能 |
|---|---|
| `_init(config)` | 从 config["buildings"] 构建决策列表（无 config 用硬编码） |
| `get_construction_decisions()` | 返回所有建造类决策 |
| `get_policy_decisions()` | 返回脱水/浸泡/大生育/配给制/工业强心剂 |
| `can_execute(id, entities, tech_tree)` | 检查冷却/科技前置/资源够不够/政策互斥 |
| `execute_decision(id, entities, tech_tree, zone_manager, zone_id)` | 扣资源 → 建造（new GameBuilding → add_building → 加入区域）或执行政策 |
| `_execute_construction(decision, entities, zone_manager, zone_id)` | 创建 GameBuilding 实例（under_construction=true） |
| `_execute_policy(id, entities)` | 脱水/浸泡切换；其他三项政策启用/结束，大生育启用时扣 500 食物 |
| `update_cooldowns(dt, time_scale)` | 游戏天数衰减冷却计时器 |
| `get_state() / load_state()` | 当前状态 + 冷却列表 + 建筑 ID 自增值 |

**脱水效果**: 食物消耗 ×0.2 / 非豁免建筑产出 ×0.1 / 科研 ×0.1 / 库存人口按区域环境逐帧损耗

**其他政策效果**: 大生育人口增长 ×3.0；配给制食物消耗 ×0.5、效率 ×0.8 并降低安定度；工业强心剂建筑与科研产出 ×2.5 并降低健康/安定度。

---

## 场景脚本功能说明

### scenes/main.gd
入口场景，`_ready()` 中 `call_deferred` 跳转到 `initial_menu.tscn`。

### scenes/main_menu/initial_menu.gd
标题画面。3 个按钮 → StartGameMenu / SettingsScreen / quit()。使用 `$MainVBox/ButtonName` 路径引用节点。

### scenes/main_menu/start_game_menu.gd
开始与命名菜单。2 种 State（MAIN / NAMING）：
- **MAIN**: 新游戏 / 继续游戏（找最新存档） / 加载存档 / 返回
- **NAMING**: LineEdit 输入宇宙名，确认/取消按钮由 HBoxContainer 自适应排列 → `GameState.new_game(name)` → 写入“初始存档” → MainScreen
- “加载存档”打开共享 `SaveBrowser`，收到 `save_selected(filepath)` 后统一调用 `SaveManager.load_game()`。

当前没有难度、引导模式或起始区域步骤；确认名称后立即进入主页面。上述扩展属于“下一阶段目标架构 C/E”，完成前不得把它记录为已实现流程。

### scenes/shared/save_browser.gd
主菜单与暂停菜单复用的全屏存档浏览器。扫描 `res://saves/` 后按宇宙分组，将每个存档生成为真实 `Button` 卡片，直接显示名称、天数和保存时间；支持单击选择、双击加载、Enter 加载、Delete 删除与 Esc 返回。工具栏可删除选中的单个存档，分组标题可删除整个宇宙，两者都在覆盖层中二次确认。

### scenes/main_menu/settings_screen.gd
4 个标签页（游戏/显示/音频/控制），通过代码动态构建 `HSlider` + `CheckBox` 控件。`_load_settings()` 从 `res://settings.json` 读取，`_save_settings()` 写入并应用全屏/vsync。游戏页包含开发者模式开关，应用后同步到 `GameState`。

“启用教程提示”目前只读写布尔设置，没有调用方或实际提示 UI；未来由目标架构 E 迁移为完整/精简/关闭三态。

### scenes/game/main_screen.gd
游戏主界面，最关键的 UI 脚本：
- `_ready()`: 若 `GameState.game_started == false` 则创建默认宇宙；连接状态刷新信号
- 模拟更新由 Autoload `GameState._process()` 持续驱动，切换子界面不会停止
- `_refresh_panels()`: 响应 `GameState.state_updated` 信号 → 重建资源面板（按组分）/ 文明面板（人口+科研）/ 环境面板（温度+辐射+光照+天数+时间倍率）
- 顶部自适应工具栏: 暂停 / `1x→2x→3x→5x` 四段 HSlider（含当前倍率文本）/ 科技树 / 政策 / 区域 / 星图；星图开局显示为锁定，望远镜科技解锁；开发者模式可绕过入口限制
- 开发者工具面板: 动态表单修改人口、库存人口、生育人口、7 类资源和 3 类科研点，并提供资源填充、全科技解锁动作
- `_input()`: `Esc` 优先关闭开发者工具，否则进入暂停菜单；空格暂停，`+/-/1/2/3/5` 与滑条双向同步

### scenes/game/game_menu.gd
暂停菜单。5 个按钮通过 `%UniqueName` 绑定 → 继续 / 保存 / 加载 / 设置 / 主菜单。居中卡片使用明确标题、副标题、独立保存状态面板与底部提示；“保存”打开命名弹层，预填“宇宙名_Day天数”并列出最近三个存档；“加载”在当前场景中打开与主菜单完全相同的 `SaveBrowser`；弹层内 `Esc` 优先关闭，菜单内 `Esc` 恢复模拟。

所有主要菜单与游戏子界面以 1920×1080 逻辑画布布局，配合 `canvas_items`/`keep` 拉伸策略适配 1080p、2.5K 及更高常规分辨率。顶部工具栏统一 24px 安全边距，底部提示使用 16px 高对比文字；区域地图和详情继续由 `HSplitContainer` 隔离并裁剪。

### scenes/starmap/starmap_view.gd
3D 星图可视化：
- `WorldEnvironment` 提供近黑深空、低强度环境光、Filmic 色调映射与辉光，`GPUParticles3D` 生成 1100 颗冷暖分色星点
- 动态 `SphereMesh`；恒星使用程序化对流表面 Shader、加色日冕与随星体移动的 `OmniLight3D`
- 行星使用受三颗恒星真实光照的程序化海陆/云层/大气边缘材质，并附加低透明度 12×6 `ImmediateMesh` 经纬网随自转旋转
- `ImmediateMesh` 绘制渐隐历史轨迹与科技分级预测轨迹
- 望远镜解锁星图；计算机/混沌预测模型逐级延长预测轨迹
- 平滑球坐标摄像机，支持自由/行星锁定模式、鼠标与 WASD/QE 控制
- 模拟层检测任意天体碰撞；发生碰撞时暂停模拟，星图显示红色文明毁灭层并可返回新游戏流程

### scenes/tech_tree/tech_tree.gd
科技树 2D 可视化：
- `tech_tree_canvas.gd` 手绘四状态节点、进度条和贝塞尔依赖线
- 左键开始/取消研究，滚轮以鼠标为中心缩放，右键拖拽平移
- 悬浮 Tooltip 完整显示效果、科研/物质需求、前置和当前可用性

### scenes/zone_view/zone_view.gd
2D 区域热力图：
- `zone_map.gd` 绘制并命中测试 12×6 温度/辐射/光照热力图
- HSplitContainer 以约 2:1 分配地图/详情宽度，MapPanel 开启 `clip_contents` 防止热力图跨栏绘制
- 选区后显示经纬、地形、环境、矿物禀赋、肥沃度、藻类密度和工作效率
- 指定区域建造，并显示施工/耐久/产出/消耗；支持工人与生育人口的 `清空/-10/-1/+1/+10/最大` 批量分配
- 右侧详情结构仅在选区或建筑列表变化时重建；0.25 秒周期刷新只原地更新 Label、按钮禁用状态和地图数据，避免按钮悬停/点击时被替换
- 空格与其他游戏子界面一致地切换模拟暂停/继续，并在底部状态栏反馈

### scenes/decision/decision.gd
文明政策界面：
- 显示文明形态、库存人口、健康与社会安定度
- 动态列出五项政策，显示效果/消耗/可用性和生效状态
- 建筑建造仅在区域界面中对当前选中区域执行
- 空格切换模拟暂停/继续并在底部提示当前状态

---

## 开发路线图

### ✅ Phase 1：骨架搭建（已完成 2026-07）
- [x] Godot 项目创建 + 3 个 Autoload 注册
- [x] 完整场景导航流程（8 个场景互联）
- [x] UI Theme（按钮/标签/面板 StyleBoxFlat）
- [x] 设置界面（4 标签页 + JSON 持久化）
- [x] 开始游戏菜单（宇宙命名 / 存档浏览器 / 删除确认）

### ✅ Phase 2：模拟核心移植（已完成 2026-07）
- [x] 三体运动模拟（RK4 + 轨迹 + 环境参数）
- [x] 72 区域系统（地形 + 自转 + 热惯性 + 扩散）
- [x] 实体管理器（资源/建筑/人口/电力/建造）
- [x] 科技树功能原型（5 层 17 节点 + 渐进注入；待下一代知识系统替换）
- [x] 决策管理器（17 种建筑 + 脱水/浸泡）
- [x] GameState 完整更新循环 + 序列化

### ✅ Phase 3：3D 渲染（已完成 2026-07）
- [x] 星图轨迹线渲染（ImmediateMesh）
- [x] 行星球体 12×6 经纬网格与自转
- [x] 粒子星空背景（GPUParticles3D）
- [x] 恒星渐变发光 ShaderMaterial
- [x] 深空环境、恒星动态表面/日冕/点光源与程序化行星材质
- [x] 摄像机平滑过渡与行星锁定

### 🚧 Phase 4：UI 交互完善（进行中）
- [x] 科技树点击交互（开始研究/取消/详情 Tooltip/缩放拖拽）
- [x] 区域浏览点击选择区域 + 建造建筑流程
- [x] 建筑详情面板（分配工人/耐久度/施工进度/产出消耗）
- [x] 五项政策的执行、取消与模拟效果
- [x] 1080p/2.5K+ 通用布局、暂停菜单与操作提示可读性
- [x] 开发者模式与人口/资源/科研/科技调试工具
- [ ] 主界面面板美化（图标/进度条/数值格式化）
- [ ] 数据告警效果（极端环境闪烁/红色滤镜）

### 🚧 Phase 5：存档 / 打包 / 打磨（进行中）
- [x] 存档流程完整接入（`save_game` / `load_game` 调用 `to_dict` / `from_dict`）
- [x] 开发期项目内 `res://saves/` 目录 + 新宇宙初始存档 + 可命名手动存档
- [x] 手动存档命名与最近存档预览 UI；文件名含时间戳，同名保存不会覆盖旧档
- [x] 主菜单/暂停菜单共享分组存档浏览器；存档可见选择、加载和单档删除
- [ ] config.yaml → Godot Resource (.tres) 迁移
- [ ] 字体资源添加（中文字体 .ttf）
- [ ] 打包导出测试（Windows / Linux）

### 📋 Phase 6：区域地表观测主页面（规划）
- [ ] 提取星图与地表天空共用的 `CelestialViewRenderer`
- [ ] 建立所选区域的当地天顶/地平坐标与地面遮挡
- [ ] 主页面改为上半天空、下半地面，数据迁移到底栏/边缘 HUD
- [ ] 增加紧凑区域选择器并保存 `observed_zone_id`
- [ ] 按六种地形提供空白地表占位与未来建筑视觉注册表
- [ ] 验证不同区域天体方位、昼夜和星图状态一致

### 📋 Phase 7：知识发现、传承与科技演化（核心玩法规划）
- [ ] 建立数据驱动 KnowledgeGraph 与八状态节点生命周期
- [ ] 接入自然观察、主动实验、生产经验、随机事件和遗迹线索入口
- [ ] 将理论研究与工程化项目分离，建筑改查能力标签
- [ ] 实现教师/学徒、记录载体、实践熟练度和按代知识衰退
- [ ] 实现知识政策树、总体/载体/领域优先级及数据驱动制度节点
- [ ] 实现教师与学生岗位、课程、师生比、教材和实践教学结算
- [ ] 将避难所拆分为生命保障、干燥库位、体积、承重与能源容量；装载预览始终可见，损失预览按文明预测能力裁剪
- [ ] 验证生命优先/均衡/知识优先在同一灾害输入下可复现，且不能突破实际容量
- [ ] 科技树按已知分支渐进显露，未知节点不泄露名称与效果
- [ ] 建立旧 17 节点科技存档迁移表和版本化节点数据测试

### 📋 Phase 8：难度、稳定纪元与真实混沌转换（规划）
- [ ] 建立版本化 `scenario_difficulties.tres`，用配置测试校验 ID、默认项、年数和自定义范围
- [ ] 新游戏难度步骤从配置生成 100/50/25/0 年默认预设和自定义稳定期，不在 UI 硬编码
- [ ] 新场景固化预设实际年数和配置版本，验证修改配置不影响已有存档
- [ ] `StableEphemerisProvider` 保证主恒星—行星二体稳定与远星规定接近
- [ ] 到期连续切换现有 RK4 N 体模拟，保存阶段、种子和转换状态
- [ ] 观测站/卫星网络为轨道预测提供数据质量与覆盖率
- [ ] 建立 `HazardForecastService`，按科技能力逐级开放定性预警、区域风险、伤亡区间与知识损失预测
- [ ] 隔离文明预测快照与真实灾害结果，验证低科技 UI 和方案差分不会泄露未来轨道
- [ ] 预测快照保存来源、生成时间与置信度；观测过期或模型改变后明确标记失效
- [ ] 验证指定年份前零提前碰撞、转换连续、极限模式从第 0 天真实模拟
- [ ] 旧存档无阶段字段时保持当前 N 体状态

### 📋 Phase 9：起始定居、开局引导与跨区系统（规划）
- [ ] 将新游戏扩展为命名→难度→引导模式→创建，并保存未完成的第 0 天定居阶段
- [ ] 建立 `OpeningGuidanceController`、版本化任务资源、完整/精简/关闭模式和手册恢复入口
- [ ] 候选区只使用公开 `ZoneKnowledgeView`，验证隐藏矿藏与未来轨道不影响推荐
- [ ] 实现区域认知五状态、真实勘探队与带置信度的勘探结果
- [ ] 将全局人口/库存一次性迁移为区域人口组、区域库存和运输中状态
- [ ] 实现相邻路线、迁徙/勘探/运输任务、补给消耗、改道和到达结算
- [ ] 实现前哨补给、聚落升级与迁都；观察区域、建造目标和首都保持独立
- [ ] 引导任务只监听领域结果，验证跳过/关闭/恢复/存读档不重复结算
- [ ] 旧存档迁移到区域 `0` 默认聚落，旧教程布尔值迁移为新引导模式

---

## 验证命令

```bash
# 验证全部脚本无编译 / 解析错误
godot --headless --quit

# 运行时测试（加载主场景 5 秒）
timeout 5 godot --headless

# 开发者工具状态修改与项目内存档往返回归
godot --headless --path . --scene tests/developer_tools_test.tscn

# 1080p 关键控件布局回归
godot --headless --path . --scene tests/ui_layout_test.tscn

# 存档浏览器可见列表、选择信号与两处入口复用回归
godot --headless --path . --scene tests/save_browser_test.tscn

# 区域按钮稳定性、人口批量分配和所有游戏子界面空格暂停回归
godot --headless --path . --scene tests/subscreen_interaction_test.tscn
```

---

*文档版本: 9.4*
*最后更新: 2026-07-14*
